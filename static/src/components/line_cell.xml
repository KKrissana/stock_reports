<?xml version="1.0" encoding="utf-8"?>
<template>
    <t t-name="stock_reports.StockReportLineCell" t-inherit="account_reports.AccountReportLineCellCustomizable">

        <xpath expr="//div[hasclass('content')]" position="replace">
            <div class="content">
                <t t-if="props.cell.auditable">
                    <a t-on-click="() => this.audit()">
                        <t t-component="env.component('AccountReportEllipsis')" t-props="{
                            name: props.cell.name?.toString(),
                            no_format: props.cell.no_format,
                            type: props.cell.figure_type,
                            maxCharacters: 'maxCharacters' in props.cell ? props.cell.maxCharacters : 50,
                        }"/>
                    </a>
                </t>
                <t t-else="">
                    <t t-component="env.component('AccountReportEllipsis')" t-props="{
                        name: props.cell.name?.toString(),
                        no_format: props.cell.no_format,
                        type: props.cell.figure_type,
                        maxCharacters: 'maxCharacters' in props.cell ? props.cell.maxCharacters : 50,
                    }"/>
                </t>
            </div>
        </xpath>
    </t>
</template>

    

